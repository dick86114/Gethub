# ==============================================
# Docker 部署配置 (用于 docker-compose.yml)
# ==============================================

# 镜像拉取源 (GitHub 用户名，用于拉取 ghcr.io 镜像)
GITHUB_REPOSITORY_OWNER=dick86114

# 本地数据库配置 (用于初始化 PostgreSQL 容器)
# 注意：修改这些值后，需要重新构建/启动数据库容器才能生效
DB_USER=gethub
DB_PASSWORD=gethub_pass
DB_NAME=gethub

# ==============================================
# 应用运行时配置 (传递给容器)
# ==============================================

# 服务监听端口
PORT=8080

# JWT 签名密钥 (建议在生产环境修改为随机长字符串)
# 如果不设置，系统将使用默认的非安全密钥
JWT_SECRET=please_change_this_to_a_random_secret_string

# ==============================================
# 远程数据库模式配置 (仅用于 docker-compose-remote.yml)
# ==============================================

# 远程数据库连接字符串
# 如果使用 docker-compose.yml (本地数据库模式)，无需配置此项，脚本会自动生成
# 格式: postgresql://USER:PASSWORD@HOST:PORT/DB_NAME?schema=public
DATABASE_URL=postgresql://username:password@your-db-host:5432/gethub?schema=public

# ==============================================
# 注意事项
# ==============================================
# 1. AI API Key (Gemini/DeepSeek 等) 和 GitHub Token 不需要在此处配置。
#    请在项目启动后，访问 Web 界面，点击右上角设置图标进入后台进行配置。
# 2. 修改此文件后，请重命名为 .env
